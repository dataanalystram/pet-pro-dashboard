

## Seed Demo Data with Images + Professional Orders UI Upgrade

### Current State
- **Inventory**: 6 products, all missing images, descriptions, SKUs, brands, and tags
- **Orders**: Empty table, no demo data
- **Orders UI**: Functional but basic -- needs polish to match the professional standard of other pages

### Changes

#### 1. Database Migration: Seed Rich Demo Data

**Update existing 6 inventory products** with real product images (from Unsplash/placeholder URLs), descriptions, SKUs, brands, tags, and retail prices where missing.

**Insert 8 demo orders** spanning different statuses (pending, confirmed, processing, shipped, delivered, cancelled) with realistic customer data, item arrays referencing real inventory products with image URLs, and varied totals/dates.

#### 2. Professional Orders Page UI Overhaul (`OrdersPage.tsx`)

Current issues:
- Stats row is plain -- upgrade with gradient backgrounds and animated counters
- Empty state is generic
- Table lacks visual hierarchy
- No date range awareness

Upgrades:
- **Refined header** with order count badge and revenue highlight
- **Stats cards** with subtle gradient icon backgrounds (matching the staff/inventory pattern)
- **Table polish**: Larger product thumbnails, status badges with dot indicators, payment status with icon, hover row highlight with left border accent
- **Mobile cards**: Add status timeline dot, better spacing, product image preview
- **Empty state**: Illustrated with call-to-action

#### 3. Professional Order Detail Dialog (`OrderDetailDialog.tsx`)

- Add order status timeline (visual stepper showing progression: Pending → Confirmed → Processing → Shipped → Delivered)
- Better item cards with larger images
- Styled totals section with background
- Payment info card

#### 4. Professional Create Order Dialog (`CreateOrderDialog.tsx`)

- Product search shows images properly
- Better visual hierarchy for the item list
- Animated total section

### Files

| Action | File | What |
|--------|------|------|
| Migration | SQL | Update 6 inventory items with images/descriptions + insert 8 demo orders |
| Rewrite | `OrdersPage.tsx` | Professional table, refined stats, polished mobile cards |
| Rewrite | `OrderStatsRow.tsx` | Gradient icon backgrounds, better visual weight |
| Modify | `OrderDetailDialog.tsx` | Status timeline stepper, better layout |
| Minor | `CreateOrderDialog.tsx` | Fix product image display in search results |

### Technical Details

**Demo product images**: Using high-quality pet product images from Unsplash CDN (`images.unsplash.com`) -- dog shampoo, cat conditioner, treats, clippers, cleaning supplies, dental kits.

**Demo orders**: 8 orders with dates spread across the last 7 days, different statuses, 1-3 items each referencing real inventory IDs and product images. Order numbers auto-generated by the DB default.

**Status timeline**: Visual stepper component showing order progression with colored dots and connecting lines. Current status highlighted, future steps grayed out.

